import { PlaceholderSection } from "@/components/app/placeholder-section";

export default function ExpensesPage() {
  return (
    <PlaceholderSection
      eyebrow="Phase 1"
      title="Expenses"
      description="Protected placeholder for expense entry and expense history."
    />
  );
}
